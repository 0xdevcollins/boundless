import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["i.postimg.cc", "res.cloudinary.com", "example.com", "images.unsplash.com","encrypted-tbn0.gstatic.com"],
	},
};

export default nextConfig;
